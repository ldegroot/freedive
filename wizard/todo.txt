Load default data set to begin with, or no? User option.
Let editor choose a chart to go with it
Chooser for div width (content area) and number of columns accordingly
Disable checkboxes on > 5
Editor choice: Default Google theme or our theme
Is embed code possible, or do we have to use iframe?
Initial query creator / data limiter
Generate both full page HTML & embed code (producer's choice)
Git repo



Name: 
    data explorer
    tangerine
    frontier
    everest
    freedive *
    snorkel
    data snorkel
    data runner
    datasnort
    
    
    
==========

These should already be in the ticket system:


https://github.com/kdmc/Freedive
Global setup:
 Set up git
  git config --global user.name "Your Name"
  git config --global user.email kdmcinfo@journalism.berkeley.edu
Add your public key
      
Next steps:
  mkdir Freedive
  cd Freedive
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:kdmc/freedive.git
  git push -u origin master
      
Existing Git Repo?
  cd existing_git_repo
  git remote add origin git@github.com:kdmc/Freedive.git
  git push -u origin master
      
Importing a Subversion Repo?
  Click here
      
When you're done:
  Continue    